'use strict'

module.exports= {
  
  TAG_REQUIRED : 'Tags parameter is required',
  INVALID_SORTBY : 'sortBy parameter is invalid',
  INVALID_DIRECTION : 'direction parameter is invalid',
  SORTBY_FIELDS: ['id', 'reads', 'likes', 'popularity'],
  DIRECTION_FIELDS: ['desc', 'asc']

}